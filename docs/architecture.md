# Architecture

PulseBoard uses a FastAPI backend with layered routers, services, repositories, schemas, and SQLAlchemy models. The React frontend is organized by feature modules with shared UI, layout, chart, store, and API client utilities.
